{
  "name": "aggelog",
  "version": "0.1.0",
  "private": true,
  "description": "아끼로그 (AggeLog) — AI bookkeeping & tax-forecasting MVP monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:web": "npm run build --workspace apps/web",
    "build:api": "npm run build --workspace apps/api",
    "build:mcp": "npm run build --workspace apps/mcp",
    "dev": "npm run dev --workspaces --if-present",
    "dev:web": "npm run dev --workspace apps/web",
    "dev:api": "npm run dev --workspace apps/api",
    "mcp": "npm run build --workspace apps/mcp && npm run start --workspace apps/mcp",
    "mcp:test": "npm run test --workspace apps/mcp",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "smoke": "bash scripts/smoke-test.sh"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "devDependencies": {
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "@types/pg": "^8.20.4",
    "pg": "^8.22.0",
    "prisma": "^7.9.1",
    "tsx": "^4.23.10"
  }
}
