{
  "name": "@aggelog/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "아끼로그 (AggeLog) — shared types & API contracts between web and api",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
