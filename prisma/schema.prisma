// 아끼로그 (AggeLog) — Prisma schema (placeholder).
// Full entities (Member, Business, Consent, Ledger, Transaction, Voucher,
// VoucherLine, Classification, TaxPrediction, Filing, Notification) are added
// in a subsequent task.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
